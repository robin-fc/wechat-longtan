<view class="entry-card {{align}} {{size}}" bindtap="onTap">
  <view class="icon-wrapper">
    <image class="icon" src="{{iconUrl}}" mode="aspectFill" />
  </view>
  <text class="title">{{title}}</text>
</view>