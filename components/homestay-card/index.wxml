<view class="homestay-card {{layout}}" bindtap="onTap">
  <image
    class="poster"
    src="{{homestay.cover.url}}"
    mode="aspectFill"
  />
  <view class="content">
    <text class="name">{{homestay.name}}</text>
    <view class="tags">
      <block
        wx:for="{{homestay.featureTags}}"
        wx:key="id"
        wx:for-item="item"
      >
        <text class="tag">{{item.name}}</text>
      </block>
    </view>
    <view class="footer">
      <view class="price">
        <text class="symbol">￥</text>
        <text class="amount">{{(homestay.referencePrice && homestay.referencePrice.amount) || homestay.minPrice || 0}}</text>
        <text class="unit">/{{(homestay.referencePrice && homestay.referencePrice.unit) || '天'}}起</text>
      </view>
      <view class="address">
        <text class="address-label">地址：</text>
        <text class="address-text">{{homestay.address}}</text>
      </view>
    </view>
  </view>
</view>

