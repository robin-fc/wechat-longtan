ai代码需要遵循以下规则：
1. 遵循已有代码的代码规范，包括命名规范、代码格式、注释规范等
2. 需要严格遵循已有架构，包括目录结构、文件组织、模块依赖等，像api/model/components等目录的封装和拆分
3. 写完代码需要复查ts类型错误问题并自行修复
4. 尽量使用后台返回的api/model封装的数据类型，前端尽量不要为了绑定数据，自定义类型
5. 严格使用es6语法，遇到异步操作，优先使用await/async，避免使用回调函数
6. 严禁.catch(() => {})后不处理异常，必须要抛出提示